/**
 * Fixed "today" the Pooja Orders mock data is authored against, so occurrence
 * statuses (Scheduled vs Awaiting completion, reassignment expiry, …) render
 * deterministically regardless of the viewer's real clock.
 */
export const ORDERS_TODAY_ISO = '2026-07-10'
